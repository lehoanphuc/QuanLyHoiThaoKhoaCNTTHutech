@using Demo1.Models
@{
    ViewBag.Title = "DanhSachDangKy";
    Layout = "~/Views/Shared/_LayoutTeam.cshtml";
}

<div>
    <div class="box">
        <h2 style="text-align:center; color: black; margin-top:10px">Danh Sách Đăng Ký</h2>
        <p>
            <a class="btn btn-primary" href="@Url.Action("DangKyHoiThao","QuanLy")" style=" margin-left: 16px">Đăng Ký Hội Thao</a>
        </p>

        <!-- /.box-header -->
        <div class="box-body">
            <table id="example2" class="table table-bordered table-hover">
                <thead style="color:black; text-align:center">
                    <tr>
                        <th>
                            Tên Đội
                        </th>
                        <th>
                            Ngày Đăng Ký
                        </th>
                        <th>
                            Trạng Thái
                        </th>
                        <th>
                            Tác Vụ
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {

                        <tr style="color:black; text-align:center">
                            <td>

                                 @item.DOI.TenDoi
                            </td>
                            <td>
                                @item.NgayDangKy
                            </td>
                            <td>
                                @if (item.TrangThai == 0)
                                {
                                    <p>Đang Chờ Duyệt</p>
                                }
                                else if (item.TrangThai == 1)
                                {
                                    <p>Đã Duyệt - Chưa Đóng Lệ Phí</p>
                                }
                                else if (item.TrangThai == 2)
                                {
                                    <p>Đã Duyệt - Đã Đóng Lệ Phí</p>
                                }   
                            </td>
                            <td>
                                @*@if (Session["TaiKhoanSV"] != null)
                                {
                                    USER user = (USER)Session["TaiKhoanSV"];
                                    if (user.MaSo == item.DOI.MaSo)
                                    {
                                        <a class="btn btn-primary" style="font-size:10px;" href="#" onclick="if (confirm('Bạn có chắc chắn muốn xoá thành viên này khỏi đội không?') == true) { location.href = '/QuanLy/XoaThanhVien?id=@item.ID' }"><i class="fa fa-fw fa-trash "></i></a>
                                    }
                                    else
                                    {*@
                                        <a class="btn btn-danger" style="font-size:10px;" href="@Url.Action("ChiTietDoi", new { id = item.MaPhieuDK })"><i class="fa fa-fw fa-list"></i></a>
                                    @*}
                                }*@


                            </td>

                        </tr>


                    }
                </tbody>
            </table>
        </div>
        <!-- /.box-body -->
    </div>
</div>


